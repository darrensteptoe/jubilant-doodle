# District Store Trace Report (2026-03-21)

Scope: store/runtime trace for failing fields without changing District UI implementation.

Trace artifact:
- `checkpoints/district_store_trace_2026-03-21.json`
- Generated by: `node scripts/trace-district-store-path.mjs`

## Race Template (`v3DistrictV2RaceType`)

Input sample:
- DOM change value: `county`

Trace sequence:
1. DOM change event value: `county`
2. Action input payload:
   - bridge input: `{ field: "raceType", rawValue: "county" }`
   - domain action payload: `{ field: "raceType", value: "county" }`
3. Canonical state immediately after action:
   - `domains.district.templateProfile.raceType = "county"`
4. Persisted payload written to storage:
   - key: `fpe/trace-campaign/trace-office/state/scenario/baseline/snapshot-v1`
   - `domains.district.templateProfile.raceType = "county"`
5. Rehydrate/load/normalize after write:
   - loaded top-level `raceType = "county"`
   - normalized top-level `raceType = "county"`
   - normalized canonical `domains.district.templateProfile.raceType = "county"`
6. Bridge canonical view value after write:
   - `template.raceType = "county"`
7. Control re-sync value on blur/refresh:
   - source: `syncSelectOptions("v3DistrictV2RaceType", ...)`
   - synced value: `county`

Result:
- First correct step: `1 (dom_change)`
- First wrong step after correct: `none observed`

## Universe Size (`v3DistrictV2UniverseSize`)

Input sample:
- DOM change value: `"123456"`

Trace sequence:
1. DOM change event value: `123456`
2. Action input payload:
   - bridge input: `{ field: "universeSize", rawValue: "123456" }`
   - domain action payload: `{ field: "universeSize", value: 123456 }`
3. Canonical state immediately after action:
   - `domains.district.form.universeSize = 123456`
4. Persisted payload written to storage:
   - key: `fpe/trace-campaign/trace-office/state/scenario/baseline/snapshot-v1`
   - `domains.district.form.universeSize = 123456`
5. Rehydrate/load/normalize after write:
   - loaded top-level `universeSize = 123456`
   - normalized top-level `universeSize = 123456`
   - normalized canonical `domains.district.form.universeSize = 123456`
6. Bridge canonical view value after write:
   - `form.universeSize = 123456`
7. Control re-sync value on blur/refresh:
   - source: `syncInputValueFromRaw("v3DistrictV2UniverseSize", ...)`
   - synced value: `"123456"`

Result:
- First correct step: `1 (dom_change)`
- First wrong step after correct: `none observed`

## Bundle / Runtime Path Audit

Checks run:
- `rg -n "script type=\"module\" crossorigin src=\"/assets/index-" dist/index.html -S`
- `rg -n "\[district_v2\] mounted|v3DistrictV2RaceType|districtV2:zJ|districtV2" dist/assets/index-*.js -S`
- `rg -n "markDistrictPendingWrite|shouldHoldDistrictControlSync|bindDistrictFormSelect\(|bindDistrictFormField\(" dist/assets/index-*.js -S`

Observed:
- Dist entry points to one hashed bundle:
  - `/assets/index-joR_1DGQ.js`
- Built bundle contains District V2 runtime markers:
  - `"[district_v2] mounted"`
  - `v3DistrictV2RaceType`
  - stage surface map includes `districtV2`
- Built bundle does **not** contain legacy pending-write/hold symbols:
  - `markDistrictPendingWrite`
  - `shouldHoldDistrictControlSync`
  - legacy `bindDistrictFormSelect/bindDistrictFormField` symbol names

Interpretation:
- Local built artifact is aligned with the new District runtime path.
- Store-level action/persist/rehydrate path did not reproduce value reversion for tested inputs.
- If reversion still occurs in a browser session, the most likely remaining causes are:
  1. Browser still executing an older cached JS/HTML asset set.
  2. Session running a different runtime build/path than local dist output.
  3. A non-store overwrite path outside this traced sequence (requires live browser console/network capture).
